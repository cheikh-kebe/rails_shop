<section id="pay">

<br>
<br>
<h2>Merci <%=current_user.username ? current_user.username.capitalize : current_user.username%>! Nous allons procéder au paiement! </h2>
<%= render 'form', order: @order %>

<br>

</section>
